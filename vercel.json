{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "frontend/build"
      }
    }
  ],
  "routes": [
    {
      "src": "/(?:[^/]+/)*[^/]+\\.[^/]+$",
      "dest": "$0"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
